# موقع فضيلة الشيخ يوسف حازم أبو غزالة

موقع إلكتروني علمي متكامل مخصص لفضيلة الشيخ يوسف حازم أبو غزالة، يهدف إلى نشر المحتوى العلمي والشرعي بأسلوب عصري وفاخر.

## 🌟 المميزات

- **تصميم إسلامي عصري**: يعتمد على ثيم "الزمرد المذهب" (Golden Emerald) المريح للعين مع واجهة تفاعلية.
- **مكتبة الدروس**: عرض منظم لسلاسل الدروس العلمية المرتبطة بقناة يوتيوب مع إمكانية الفرز حسب التصنيف.
- **ركن الفتاوى**: أرشيف للأسئلة والأجوبة الشرعية مع نظام بحث سريع مدمج.
- **المؤلفات**: عرض تفصيلي للكتب والتحقيقات العلمية مع روابط التحميل والشراء.
- **الصفحات القانونية**: يتضمن الموقع الآن صفحات مخصصة لسياسة الخصوصية والشروط والأحكام.
- **لوحة تحكم (Admin)**: واجهة مخصصة لإدارة المحتوى (دروس، فتاوى، كتب) محمية بنظام Clerk مع توجيه تلقائي ذكي.
- **سرعة فائقة**: تحسين الصور والأداء باستخدام أحدث تقنيات Next.js 16 & React 19.

## 🛠 التقنيات المستخدمة

- **Framework**: [Next.js 16 (App Router)](https://nextjs.org/)
- **Library**: [React 19](https://react.dev/)
- **Styling**: [Tailwind CSS 4](https://tailwindcss.com/)
- **Authentication**: [Clerk](https://clerk.com/)
- **Database**: [Supabase (PostgreSQL)](https://supabase.com/)
- **Animations**: [Framer Motion](https://www.framer.com/motion/)
- **Icons**: [Lucide React](https://lucide.dev/)
- **Monitoring**: [Sentry](https://sentry.io/)

## 🚀 البدء بالعمل

### 1. المتطلبات

- Node.js 18+
- حساب على Clerk و Supabase و Sentry.

### 2. إعداد متغيرات البيئة

قم بإنشاء ملف `.env.local` وأضف المفاتيح التالية:

```env
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=...
CLERK_SECRET_KEY=...
NEXT_PUBLIC_SUPABASE_URL=...
NEXT_PUBLIC_SUPABASE_ANON_KEY=...
```

### 3. التثبيت والتشغيل

```bash
# تثبيت المكتبات
npm install

# تشغيل السيرفر المحلي
npm run dev

# بناء المشروع للإنتاج
npm run build
```

## 📂 هيكلة المشروع

- `src/app`: تحتوي على الصفحات والموجهات (App Router).
  - `admin`: لوحة التحكم (إدارة الدروس، الفتاوى، الكتب).
  - `privacy`: صفحة سياسة الخصوصية.
  - `terms`: صفحة الشروط والأحكام.
- `src/components`: المكونات البرمجية القابلة لإعادة الاستخدام.
  - `LessonsList`: مكون عرض الدروس مع نظام الفلترة.
  - `FatwasList`: مكون عرض الفتاوى مع نظام البحث.
  - `BooksList`: مكون عرض المؤلفات مع المؤثرات البصرية.
- `src/proxy.ts`: نظام الحماية والتوجيه التلقائي (Middleware).
- `supabase/schema.sql`: هيكلية قاعدة البيانات.

## 🗄️ إعداد قاعدة البيانات

لإعداد جداول البيانات المطلوبة، قم بنسخ المحتوى الموجود في `supabase/schema.sql` وتشغيله في **SQL Editor** داخل لوحة تحكم Supabase.

> [!IMPORTANT]
>
> 1. **إضافة الأعمدة**: تأكد من وجود عمود `category` في جدول `content`.
> 2. **سياسات الأمان (RLS)**: يجب تشغيل قواعد الأمان الموجودة في نهاية ملف `schema.sql` للسماح بإضافة وتعديل البيانات من لوحة التحكم، وإلا ستواجه خطأ "row-level security policy".

## 🛠️ استكشاف الأخطاء وإصلاحها

### 1. تحذير Clerk (Development Keys)

إذا ظهر تحذير "Clerk has been loaded with development keys" في المتصفح، فهذا يعني أن التطبيق يستخدم مفاتيح بيئة التطوير.

- **الحل**: عند النشر للإنتاج (Production)، قم بتغيير المفاتيح في إعدادات البيئة إلى مفاتيح الإنتاج من لوحة تحكم Clerk.

### 2. خطأ Supabase (400 Bad Request)

إذا ظهر خطأ 400 عند محاولة إضافة أو عرض المحتوى:

- **السبب**: غالباً ما يكون بسبب عدم تطابق الأعمدة بين الكود وقاعدة البيانات (مثل نقص عمود `category` في جدول `content`).
- **الحل**: تأكد من تحديث الجداول في Supabase باستخدام النسخة الأخيرة من ملف `supabase/schema.sql`.

## 📄 الترخيص

حقوق الطبع والنشر محفوظة لأسرة الشيخ يوسف حازم أبو غزالة © 2026.
تطوير وإشراف: فريق التحول الرقمي.
